---
name: mistral_get_build_hosts
description: Mistral get build hosts subworkflow
enabled: true
runner_type: mistral-v2
entry_point: workflows/mistral_get_build_hosts.yaml
parameters:
  hosts:
    type: object
    properties:
      test:
        type: string
        description: The name of the host where the integration test(s) will run.
        required: true
      build:
        type: string
        description: The name of the host where the build process will run.
